
def random_math_homework():
    # Generate a random equation to solve
    equation = f"{random.randint(1, 10)} + {random.randint(1, 10)}"
    
    # Solve the equation and return the result
    return eval(equation)